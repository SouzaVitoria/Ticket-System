import { BrowserRouter } from "react-router-dom"
import RoutesAll from "./routes"

const App = () => {
  return (
    <BrowserRouter>
      <RoutesAll />
    </BrowserRouter>
  );
}

export default App;
